[package]
name = "genshin_pinyin_sort"
version = "1.1.1"
edition = "2021"

[profile.release]
strip = true
lto = true
panic = "abort"

[dependencies]
pinyin = "0.10.0"
chrono = "0.4.41"

version_lib = { path = "./version_lib" }
exchange_lib = { path = "./rename_lib" }
